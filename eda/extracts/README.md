## Extracts

This directory follows a Bronze/Silver template:

- `bronze/`: immutable raw extracts pulled from retrieval APIs.
- `silver/`: cleaned/typed/feature-ready extracts derived from bronze (created when EDA run uses `--build-silver`).

Only the directory templates are committed; extracted data files are local artifacts.
