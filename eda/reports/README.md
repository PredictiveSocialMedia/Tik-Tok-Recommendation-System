## Reports

Store finalized EDA outputs here:

- PNG/SVG figures
- Markdown summaries
- Slide exports
- Automated latest summary at `latest/summary.md`

Include run metadata in each report (run_id + manifest path) so results are reproducible.
